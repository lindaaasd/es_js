let myFirstArray = [1, 2, 3, 4, 5];

function minArray (tt){
    let mini = Math.min(...tt)

    result = console.log ("Il valore minore della fantastica lista è " + mini)
    }

function maxArray (pp){
let maxi = Math.max (...pp)

result = console.log ("Il valore più grande della fantastica lista è " + maxi)
}

minArray (myFirstArray)
maxArray (myFirstArray)